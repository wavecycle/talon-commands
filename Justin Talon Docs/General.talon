-
settings():
    key_wait = 8.0

windows shutdown:
    key(super-x)
    key(u)
    key(u)

grab left:
    key(ctrl-shift-left)
grab right:
    key(ctrl-shift-right)
grab up:
    key(shift-up)
grab down:
    key(shift-down)
    
jump left:
    key(ctrl-left)
jump right:
    key(ctrl-right)
# jump up:
#     key(ctrl-up)
# jump down:
#     key(ctrl-down)